apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: kubeflow
type: Opaque
stringData:
  # Change this password in production!
  password: "kubeflow123"
  root-password: "kubeflow123"
  # Database name
  database: "kubeflow_db"
  # Database user
  user: "kubeflow"
