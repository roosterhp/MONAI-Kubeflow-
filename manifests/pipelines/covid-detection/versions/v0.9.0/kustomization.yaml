apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubeflow

resources:
  - pipeline.yaml

commonLabels:
  pipeline: covid-detection
  version: v0.9.0
  model: baseline

commonAnnotations:
  git-sha: "baseline"
  build-date: "2025-12-28"
