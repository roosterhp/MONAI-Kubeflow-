# PLACEHOLDER: Kubeflow Pipeline Compiled YAML
#
# This file needs to be generated by compiling hospital-mlops/covid-demo/pipeline.py
#
# To generate this file:
# 1. Activate Python environment with KFP SDK installed
# 2. Navigate to hospital-mlops/covid-demo/
# 3. Run: python -c "from kfp import compiler; from pipeline import covid_pipeline; compiler.Compiler().compile(covid_pipeline, 'pipeline-compiled.yaml')"
# 4. Copy pipeline-compiled.yaml to this location
#
# Or use GitHub Actions workflow which will automatically compile and update this file
#
# Expected structure: Kubeflow Pipeline v2 YAML with components, steps, and parameters
